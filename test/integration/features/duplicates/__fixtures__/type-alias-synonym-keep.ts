// K: 명명된 타입에 대한 bare 별칭(synonym) — 자기 멤버 구조가 없고 대상 타입을 따라가므로
// 독립적으로 드리프트할 결정이 없다 (빈 marker 타입과 같은 부류의 골격). 보고 금지.
type AnalyzeFormatInput = ToolAnalysisInput;

type AnalyzeLintInput = ToolAnalysisInput;
