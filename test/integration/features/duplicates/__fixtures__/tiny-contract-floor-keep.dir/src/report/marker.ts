// Unrelated module that coincidentally converged on the same universal shape.
// No coupling obligation exists between the two — below-floor contract → K.
export interface MarkerPoint {
  line: number;
  column: number;
}
