// K: 평탄한 스칼라 리스트의 반복은 "단일 상수 값의 반복"(redundancy/상수 추출 영역, 비대상)
// 이지 규칙 데이터(매핑·룩업 테이블)가 아니다. 룩업 테이블은 행(row)·키→값 매핑이어야 한다.
const PRIMARY_NAMES = ['duplicates', 'waste', 'barrel', 'error-flow', 'format', 'lint'];
