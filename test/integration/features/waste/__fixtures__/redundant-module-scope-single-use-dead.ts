// DEAD (Phase2, module scope): CLAUDE.md "모든 scope" — a module-level single-use
// const with a pure expression initializer is redundant just like in function scope.
const base = 40 + 1;
export const total = base;
