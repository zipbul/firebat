export function computeStats(): number {
  const total = 0;

  const adjusted = total + 10;
  const scaled = adjusted * 2;
  const offset = scaled - 5;
  const result = offset / 3;

  return result;
}
